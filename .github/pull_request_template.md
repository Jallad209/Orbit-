## What

<!-- One or two sentences. Which week/track does this belong to? -->

## Checklist

- [ ] Tests added or updated, and `pnpm run test` passes locally
- [ ] `pnpm run type-check` and `pnpm run lint` pass
- [ ] No network dependency introduced into the app (fonts, assets, and libraries are bundled)
- [ ] Tauri APIs, if any, are used only inside `apps/orbit/src/platform/`
- [ ] The relevant `docs/*-TASKS.md` status and "What was done" are updated
- [ ] Schema changes: export schema version bumped and a migration added
